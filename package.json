{
  "name": "webperf-hub",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "corepack pnpm -r build",
    "dev:api": "corepack pnpm --filter @webperf/api dev",
    "dev:web": "corepack pnpm --filter @webperf/web dev",
    "profile:pages": "corepack pnpm --filter @webperf/api profile:pages",
    "test": "corepack pnpm -r test",
    "lint": "corepack pnpm -r lint"
  }
}
